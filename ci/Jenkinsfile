pipeline {
    agent {
        docker {
            image 'my-fedora-k6'
        }
    }

    stages {
        stage('Run K6') {
            steps {
                sh 'k6 version'  // Verify the installation
            }
        }
    }
}
