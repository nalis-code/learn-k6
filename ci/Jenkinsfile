pipeline {

    agent { dockerfile true }

    stages {
        stage('go tests'){
            steps {
                    sh 'go test -v'
            }
        }
        stage('k6 Tests') {
            steps {
                    sh 'k6 run tests/smoke-test.js'
            }
        }
    }
}
