/* Requires the Docker Pipeline plugin */
pipeline {
     agent any

    stages {
        stage('build') {
            steps {
                sh 'go version'
            }
        }
         stage('k6') {
            steps {
                sh 'k6'
            }
        }
}
}