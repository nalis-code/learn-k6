pipeline {

    agent { dockerfile true }

    stages {
        parallel[(
        stage('functional tests'){
            steps {
                    sh 'echo "go test"'
            }
        }
        stage('performance Tests') {
            steps {
                    sh 'k6 run tests/smoke-test.js'
            }
        }
        )]
        
    }
}
